let pontos = 85;


//vitoria ((=>30) && (<=40)) || ((>=50) && (<=80))

if ( (pontos>=30) && (pontos<=48) || (pontos>=70) && (pontos<=80) ){
console.log('ganhou')}
  else{
    console.log('perdeu')
  
 }